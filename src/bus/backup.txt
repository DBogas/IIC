//ver todos os ficheiros que existem na pasta em questão
		//File input = new File("/home/diogo/workspace/iic/webcrawler/input/200.html");
		File input = new File("/home/diogo/workspace/iic/webcrawler/input/");// caminho com os html todos
		
		//iterar sobre cada ficheiro
		Document doc = Jsoup.parse(input,"UTF-8","");
		Elements aux = doc.select("tr");
		for(Element e : aux){
			//ver se a row tem a zona das paragens, paragens abaixo pertencem a esta zona
			if(e.getElementsByTag("span")!=null){
				System.out.println(e.getElementsByTag("span").text());
			}
			Elements e_children = e.children();
			for(Element k :e_children){
				if(k.attr("class").equals("bus-location") || k.attr("class").equals("bus-code"))
						System.out.print(k.text()+",");
			}
			System.setOut()
		}